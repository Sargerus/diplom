@model IEnumerable<WebApplication1.Models.Project_User>

@{
    ViewBag.Title = "Index";
    Layout = "~/Views/Shared/_Layout.cshtml";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.isPM)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.isLead)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.isDev)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.isPM)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.isLead)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.isDev)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.ProjectId }) |
            @Html.ActionLink("Details", "Details", new { id=item.ProjectId }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.ProjectId })
        </td>
    </tr>
}

</table>
